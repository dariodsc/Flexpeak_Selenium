let numero = 0;             // define a variavel para que ela seja iniciada
while (numero <= 10) {      
  if (numero % 2 === 0) {
    console.log(numero);    
  }
  numero++;                 // Incrementa a variavel até que a condição seja atendida
}
