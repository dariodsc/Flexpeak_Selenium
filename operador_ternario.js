let idade = 19

idade >= 18 ? console.log('É maior de idade!') : console.log('Não é maior de idade!')

// O exemplo acima se refere ao operador ternário
// De certa forma, substitui o if e else (Não sei explicar ainda rs)
// ? indica se a condição é True e qual será a resposta do sistema
// : se a primeira condição não for atendida, entra no segundo operador que define a proxima resposta
